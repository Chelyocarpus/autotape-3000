export type {
  MediaFormat,
  DuplicateAction,
  TrimPreset,
  GsmtcTrack,
  UserSettings,
  SourceSessionOption,
  AudioDevice,
  RecordingEntry
} from '../../../shared/types'

export type RecordingState = 'idle' | 'recording'
